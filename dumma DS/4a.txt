import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import r2_score

dataset = pd.read_csv("experience.csv")

X = dataset.iloc[:, :-1].values
y = dataset.iloc[:, 1].values

X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=0
)

print("Training Features:\n", X_train)
print("Training Labels:\n", y_train)

model = LinearRegression()
model.fit(X_train, y_train)

print("Slope (Coefficient):", model.coef_)
print("Intercept:", model.intercept_)

y_pred = model.predict(X_test)
print("Predicted Salaries:", y_pred)

plt.scatter(X_train, y_train, color="red", label="Training Data")
plt.plot(X_train, model.predict(X_train), color="blue", label="Regression Line")
plt.title("Salary vs Experience (Training Set)")
plt.xlabel("Years of Experience")
plt.ylabel("Salary")
plt.legend()
plt.show()

accuracy = r2_score(y_test, y_pred)
print("Accuracy (R² score):", accuracy)

lin_pred = model.predict([[6.5]])
print("Predicted salary for 6.5 years of experience:", lin_pred[0])
